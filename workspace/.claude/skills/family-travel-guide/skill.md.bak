# family-travel-guide: 亲子旅游伴学手册生成器

**描述**: 基于《亲子旅游伴学手册策划方案》，为家长生成16页横向翻页HTML手册。输入景点名称+孩子年龄段，输出风格统一、知识准确、互动有趣的伴学内容。

**核心特性**: 联网核实准确性、视觉基因提取、景点文化强关联IP、16页固定框架、跨设备适配。

**触发词**: 旅行伴学、景点讲解、亲子旅行、带孩子玩、旅行攻略、家庭旅行、景点伴学、亲子游手册、伴学手册、游学PPT。

---

## 核心定位

这不是一次性做一本手册，而是一套**可复用的设计方法论**：

```
输入：景点名称 + 孩子年龄段
输出：16页横向翻页 HTML 手册
```

**设计哲学**: 页型骨架永远不变，只有"皮肤"（配色/纹样/IP角色）随景点变化。

**设计基调**: 高级感 + 童趣 + 景点强相关。拒绝 AI 默认审美（紫色渐变、纯白圆角卡片、通用卡通贴纸）。

---

## 最高优先级铁律：内容必须准确

**不可妥协的规则**——任何具体知识点在写入手册前，必须先联网检索权威信息源核实。

**强制执行流程**：

1. **联网核实**：历史事件、人物、年代、数字、建筑细节等，必须检索景点官网、博物馆官方资料、权威出版物
2. **不确定就换**：无法确认准确性的细节，宁可换一个能确认的知识点
3. **野史标注**：民间传说与正史不符时，必须明确标注为"传说"或"民间故事"
4. **数字是雷区**：数量、年代、长度、重量等是错误率最高，必须逐一核实
5. **专有名词核对**：人名、朝代、年号等必须准确

---

## 必需输入参数

| 参数 | 说明 | 示例 |
|---|---|---|
| 景点名称 | 必填 | 故宫、敦煌莫高窟、西安兵马俑 |
| 孩子年龄段 | 必填，默认6-12岁 | 11岁 |
| 输出格式 | 可选，默认HTML | HTML |

---

## 16页固定框架（骨架层，不变）

| 页码 | 页型 | 功能说明 |
|---|---|---|
| P1 | 封面页 | 景点名 + IP导览员登场 + 集章进度条预览 |
| P2 | 行前须知页 | 开放时间、路线建议、注意事项（任务清单形式） |
| P3–P4 | 总览地图页 | 手绘风格景区地图，标注知识点位置 |
| P5–P12 | 知识互动页（核心，重复8次） | 每页聚焦一个知识点 |
| P13 | 趣味测验页 | 选择题/判断题回顾知识点 |
| P14 | 涂色/手绘页 | 留白线稿供涂色/临摹 |
| P15 | 集章总览页 | 汇总任务完成情况，颁发"XX小专家"称号 |
| P16 | 家长指南页 | 延伸知识与亲子互动话术建议 |

**知识互动页（P5–P12）四段式结构**：
```
顶部页眉：页码徽章 + 单元名（中文+英文） + 进度点指示器
中部内容区：左侧插画区｜右侧知识讲解区（标题+正文+小知识卡）
底部互动任务条：图标 + 任务标题 + 任务描述 + 答题/打卡区
页脚：IP导览员一句话提示 + 翻页箭头
```

---

## 视觉基因提取四步法（核心方法论）

每次更换景点必须重新执行以下四步：

### 第一步：色彩提取

研究该景点最具代表性的真实颜色，提炼 4–6 个命名色板：

| 色位 | 功能 | 故宫示例 | 长城示例 |
|---|---|---|---|
| 主色 | 最强烈的标志性颜色 | 宫墙红 #9D2342 | 砖灰青 #5D6D7E |
| 点缀色 | 金/亮色或第二标志色 | 琉璃金 #C99B3A | 土黄 #D4A574 |
| 对比辅助色 | 差异化区块 | 石青 #2F5C6B | 烽火红 #C0392B |
| 背景纸色 | 替代纯白，体现历史质感 | 暖宣纸 #F3E9D6 | 古纸米黄 #F5F0E6 |
| 互动点缀色1 | 任务条、强调框 | —— | 松石绿 #5499C7 |
| 互动点缀色2 | — | —— | 金戈色 #F5B041 |

**执行方法**：检索该景点的真实建筑/文物/景观图片，从实际资料中提取色值。

### 第二步：图形纹样提取

找出该地 1–2 个最具识别性的装饰纹样或图形符号：

| 景点 | 纹样示例 |
|---|---|
| 故宫 | 回纹 / 云纹 |
| 江南园林 | 漏窗纹 / 冰裂纹 |
| 敦煌 | 藻井纹 / 飘带纹 |
| 长城 | 城砖纹 / 烽烟纹 |

这些纹样将用于边框装饰、分隔线、角标语言。

### 第三步：IP导览员设计

**为每个景点设计一个与当地文化强关联的吉祥物角色**，而非固定IP。

| 景点 | IP角色示例 | 一句话人设 |
|---|---|---|
| 故宫 | 瑞兽"小麒麟" | 调皮但博学，爱抖小包袱 |
| 敦煌 | 飞天"小飞" | 轻盈灵动，说话爱用"飘"字 |
| 长城 | 烽烽（小烽火） | "长城上的通信小队长，跑得比消息还快！" |
| 兵马俑 | 陶俑"阿士" | "守了2000年，最会讲老故事！" |

**IP是手册"容易被记住"的核心机制**——孩子记住的是角色和故事线，知识点是角色带出来的自然结果。

**出现规则**（5个关键页，避免刷屏）：
| 页 | 位置 | 作用 |
|---|---|---|
| 封面 | 主内容区 | "我是XX，今天一起看..." |
| 总览地图 | 提示区域 | 介绍本章主题 |
| 知识互动页（每页） | 页脚 | 一句话提示 |
| 测验页 | 提示区域 | 鼓励语 |
| 集章总览 | 主内容区 | "恭喜完成！" |

### 第四步：字体与边框语言

- **标题字体**：书法感/手绘感衬线体，加粗、宽字距，营造"匾额/题字"质感
- **正文字体**：圆润清晰的无衬线体，保证 6 岁孩子也能轻松阅读
- **边框语言**：统一采用"双层裱边 + 四角纹样角标"的"手卷/文牒"视觉语言

---

## 知识点互动设计公式

每个知识互动页的内容必须遵循以下公式构建：

```
一个具体可观察的真实细节
  + 一个孩子会觉得"哇，没想到"的反常识冷知识（须核实来源）
  + 一个在现场可以实际执行的小任务（找一找 / 数一数 / 连连看 / 听一听）
```

**任务设计双轨适配 6–12 岁**：
- 基础任务（6-8岁）：观察、指认、简单计数
- 进阶任务（9-12岁）：推理、记录、图文创作

---

## 插画生成规范

**提示词要素**：
1. **风格基调关键词**：基于视觉基因调整
2. **色板约束**：使用提取的具体 hex 色值
3. **构图留白要求**：标注需要留白的区域
4. **长宽比规格**：封面横向，知识点页接近方形或竖向
5. **风格一致性锚点**：重复引用已确定的风格关键词与色板

**插画类型**：
- 封面建筑全景类
- IP导览员角色类（多表情/姿态变体）
- 知识点场景插画类
- 互动找一找场景图类

---

## 技术实现规范（防呆代码骨架）

### 1. 响应式缩放容器（固定 1920×1080 画布）

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>亲子伴学手册</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            background: #1a1a1a;
            overflow: hidden;
            font-family: "PingFang SC", sans-serif;
        }

        /* 固定尺寸容器 */
        #container {
            position: fixed;
            top: 0;
            left: 0;
            width: 1920px;
            height: 1080px;
            background: var(--bg-color);
            overflow: hidden;
        }

        /* 响应式缩放包装器 */
        #scaler {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            transform-origin: center center;
        }

        .slide {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: none;
        }
        
        .slide.active {
            display: flex;
        }
    </style>
</head>
<body>
    <div id="scaler">
        <div id="container">
            <!-- 所有页面内容 -->
        </div>
    </div>

    <script>
        function scaleContainer() {
            const windowWidth = window.innerWidth;
            const windowHeight = window.innerHeight;
            const targetWidth = 1920;
            const targetHeight = 1080;
            const scaleX = windowWidth / targetWidth;
            const scaleY = windowHeight / targetHeight;
            const scale = Math.min(scaleX, scaleY);
            document.getElementById('scaler').style.transform = `translate(-50%, -50%) scale(${scale})`;
        }
        window.addEventListener('load', scaleContainer);
        window.addEventListener('resize', scaleContainer);
    </script>
</body>
</html>
```

**关键原则**：
- ✅ 使用 `transform: scale()` 而不是 `vw/vh` 单位
- ✅ 固定画布 1920×1080
- ❌ **禁止使用** `vw` 单位做布局

### 2. 图片处理规则

**原则**: 只使用外部可访问的永久URL。

```css
img {
    max-width: 100%;
    height: auto;
    object-fit: cover;
}
```

**图片来源优先级**：
1. **最佳**: 公共 CDN（Unsplash、Wikimedia Commons）
2. **可接受**: 已上传的永久链接
3. **禁止**: 平台临时链接

### 3. IP气泡安全写法

```css
/* 气泡容器 - 必须有明确宽度 */
.ip-wrapper {
    position: relative;
    min-width: 280px;
    max-width: 400px;
    z-index: 10;
}

.ip-bubble {
    position: absolute;
    top: -70px;
    right: -15px;
    width: 260px;
    padding: 12px 18px;
    background: white;
    border-radius: 18px;
    box-shadow: 3px 3px 0 var(--accent-color);
    border: 3px solid var(--primary-color);
    font-family: var(--hand-font);
    font-size: 16px;
    line-height: 1.4;
    z-index: 10;
}
```

**关键原则**：
- ✅ 父容器必须有 `min-width`
- ✅ 气泡本身明确指定 `width`

---

## 完整工作流程

### Step 1: 联网核实准确性
- 检索景点官方网站、博物馆官方资料
- 核实历史、年代、数字等关键信息
- 记录可靠信息来源

### Step 2: 提取视觉基因
- 第一步：色彩提取（4-6个命名色板）
- 第二步：纹样提取（1-2个识别性纹样）
- 第三步：IP导览员设计（景点文化强关联）
- 第四步：字体与边框语言

### Step 3: 设计16页内容
- P1 封面页
- P2 行前须知页
- P3-P4 总览地图页
- P5-P12 知识互动页×8（四段式结构）
- P13 趣味测验页
- P14 涂色/手绘页
- P15 集章总览页
- P16 家长指南页

### Step 4: 生成HTML输出
- 固定画布 1920×1080
- 响应式 `transform: scale()`
- 翻页交互（左右滑动、点击箭头、键盘）

### Step 5: 质量检查
- [ ] 所有知识点已联网核实
- [ ] 色彩来自景点真实资料
- [ ] IP与景点文化强关联
- [ ] 每个知识互动页包含：细节+冷知识+可执行任务
- [ ] 16页框架完整
- [ ] HTML可在手机和电脑上正常浏览

---

## 质量自检清单

### 视觉
- [ ] 配色使用提取的色板，无硬编码
- [ ] 纹样来自景点真实资料
- [ ] IP与景点文化强关联
- [ ] 字体分层使用（标题衬线/正文无衬线）

### 内容
- [ ] 16页框架完整
- [ ] 8个知识互动页四段式结构正确
- [ ] 每个知识点有互动任务
- [ ] 家长指南包含延伸知识

### 技术
- [ ] 单文件HTML
- [ ] 响应式缩放正常
- [ ] 翻页交互可用
- [ ] 图片使用外部永久URL

---

## 输出文件命名

```
{景点名}_伴学手册_{年龄}岁.html
```

示例：`长城伴学手册_11岁.html`

---

## 工具依赖与可移植性

### 核心功能（无依赖）

本 Skill 的核心功能**不依赖任何外部工具**，可直接使用：

- ✅ 16页固定框架生成
- ✅ 视觉基因提取（色彩、纹样、IP设计、字体边框）
- ✅ 知识点互动设计
- ✅ HTML 输出与响应式缩放
- ✅ 翻页交互（键盘、点击、滑动）

### 内置联网搜索能力（已融合）

本 Skill 已将联网搜索能力融合为核心功能：

**使用方法**（直接在执行时使用）：
```bash
# 联网检索景点信息
WebSearch "{景点} 官方介绍 历史"
WebSearch "{景点} 长度 数据 官方"
WebSearch "{景点} 传说 vs 正史"
```

**搜索关键词模板**：
| 目标 | 关键词模板 |
|------|------------|
| 基础信息 | `{景点} 官方介绍` |
| 准确数据 | `{景点} 历史 年代 准确信息` |
| 数字核实 | `{景点} 长度 数量 官方数据` |
| 传说区分 | `{景点} 传说 正史 区分` |

**信息来源优先级**：
1. 景区官方网站、博物馆官网
2. 权威出版物、学术资料
3. 政府网站、教育机构

**降级方案**（如果环境不支持 WebSearch）：
- 使用训练数据中的常识
- 在输出中标注"建议实地核实"
- 不确定的信息选择省略而非编造

### 可选增强功能

以下功能需要对应工具，但**非必需**：

| 工具 | 作用 | 无工具时的降级方案 |
|------|------|-------------------|
| **image_generate** | 生成IP角色、涂色线稿、场景插画 | 使用 emoji 组合或纯 CSS 图形替代 |
| **coze-file-send** | 发送HTML文件给用户 | 直接输出文件路径，用户自行获取 |

### 设计原则

**Skill 设计为"自包含"优先**：

1. **核心流程不依赖外部工具**——即使没有任何工具/Skill，也能生成完整的伴学手册
2. **工具作为"增强器"**——有工具时提供更好的体验（如联网核实、AI生图），无工具时也能正常工作
3. **明确标注工具依赖**——在文档中清楚说明哪些功能需要对应工具

### 使用建议

**对于其他用户**：

- **最小使用**：直接调用 Skill，无需安装任何工具
- **完整体验**：可选择性安装 web-access（联网核实）和 image_generate（AI生图）
- **团队协作**：工具安装在团队层面，成员共享使用

**结论**：其他用户使用本 Skill **不需要**安装任何工具即可使用核心功能。工具是**可选增强**，非必需。
